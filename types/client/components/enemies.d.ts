/**
 * Component for rendering enemies on SVG
 *
 * @argument {HTMLElement} targetElement
 * @argument {import('../data/initial-state.js').State} state
 * @returns {HTMLElement}
 */
export function enemiesComponent(targetElement: HTMLElement, state: import('../data/initial-state.js').State): HTMLElement;
