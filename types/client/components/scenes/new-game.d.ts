/**
 * Component to render new game scene if active
 *
 * @argument {HTMLElement} targetElement
 * @argument {import('../../data/initial-state.js').State} state
 * @returns {HTMLElement}
 */
export function newGameSceneComponent(targetElement: HTMLElement, state: import('../../data/initial-state.js').State): HTMLElement;
