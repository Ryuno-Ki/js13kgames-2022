/**
 * Component to render win scene if active
 *
 * @argument {HTMLElement} targetElement
 * @argument {import('../../data/initial-state.js').State} state
 * @returns {HTMLElement}
 */
export function winSceneComponent(targetElement: HTMLElement, state: import('../../data/initial-state.js').State): HTMLElement;
